# DECISIONS — byrachelpierce-web

> Running log of judgment calls. Format: id · date · question · choice · why. The agent appends; the operator rules on anything OPEN. Never delete entries — supersede them. Entries 001–012 are the planning session's calls; merging the planning PR approves them except where marked ⚠ VETO POINT (rule on those explicitly).

**001 · 2026-07-03 · Doc strategy.** New `SITE-ARCHITECTURE-v2.md` + `FINAL-BUILD-SPEC.md` supersede; old trail spec archived to `docs/old/` unchanged **except** the committed live Resend key was redacted (security beats archive-fidelity; the key is in git history anyway and is rotated in Phase 0.1). (Operator selected supersede+archive.)

**002 · 2026-07-03 · Ship line.** Rigor + finish current features + go-live (R0–R5). AR tool fully specced now (Architecture §13) but ships next release. (Operator-selected.)

**003 · 2026-07-03 · Leaked-secret handling.** Rotate both credentials (Resend key committed in the v1 spec; Turso token in `Database Token.txt`); no git-history rewrite. Why: rotation kills the risk; history surgery on a shared repo adds risk for cosmetic gain. Rotation is Operator Phase 0.1 and gates everything.

**004 · 2026-07-03 · Trail fix shape.** New `trail_completions` table (user PK, unique code, `completedAt`, `redeemedAt`) replaces the sentinel-row mechanism; migration copies then deletes `mural_id = 0` rows. Rejected: filtering sentinel rows in reads (leaves a lying table shape to trip every future query). **Includes the not-yet-used `redeemedAt` column** so the next-release cashier feature (Appendix A.3) needs no second migration. ⚠ VETO POINT if you'd rather keep the schema minimal.

**005 · 2026-07-03 · Collection rendering.** `/collection` + `/collection/[category]` become `force-dynamic` (searchParams demand it); painting pages stay SSG. Rejected: path-segment pagination (`/page/2`) — more churn for equal outcome at this traffic. (Architecture §2.)

**006 · 2026-07-03 · Code prefix stays `BRP-`.** The v1 spec said `RP-` but shipped code issued `BRP-` codes; changing now would create two families of valid-looking codes at the register. Alphabet/CSPRNG fixed per Architecture §4.2.

**007 · 2026-07-03 · Fabricated mural content is suppressed until R4.** ⚠ VETO POINT — visible behavior change. Trail UI/map/emails show real business-location names instead of the invented mural titles/years/descriptions, until Rachel's real content lands via intake. Why: Iron Invariant 3 (no fiction presented as fact on the public site). Veto = keep showing placeholders until R4 replaces them.

**008 · 2026-07-03 · Images: Vercel Blob** (operator selected the object-storage option; Blob chosen over R2 because the site already lives on Vercel — one dashboard, no new account, free tier covers 205MB). Local dev serves from the gitignored `public/art/` via the `NEXT_PUBLIC_ART_BASE_URL` switch, so dev needs no cloud setup.

**009 · 2026-07-03 · Test stack.** Vitest (+ V8 coverage, thresholds 80% on `src/lib` + `src/app/api`), Testing Library + happy-dom for components, Playwright (chromium) for e2e from R3. All DB access in tests via local libSQL `file:` databases built from `drizzle/` migrations; production Turso is unreachable from tests/CI by construction (no token present).

**010 · 2026-07-03 · npm stays** (not pnpm), lockfile committed in R0, `next-auth` pinned exactly at `5.0.0-beta.25` (two prior adapter breakages = upgrades are escalations, Spec §13.3).

**011 · 2026-07-03 · Analytics = Vercel Analytics**, not GA4. Why: zero-config, no consent-banner obligation, gallery-scale insight needs. Veto if the business wants GA4's depth.

**012 · 2026-07-03 · Baseline migration approach.** R0 snapshots the current schema as `drizzle/0000_*.sql` and the operator marks it applied on production (drizzle baseline procedure) rather than hand-writing SQL to match. Why: file DBs for tests/CI need buildable-from-migrations; production must never be re-created.

**013 · 2026-07-04 · Phase 0 exceptions ruled by operator: secret rotation deferred, branch protection substituted.**

- **0.1 (rotate Resend key + Turso token) is deferred** — back-burnered by the operator, not blocking R0 start. `Database Token.txt` still exists on disk and the old Resend key has not been confirmed revoked as of this entry. This does NOT relax Iron Invariant 2 (no secrets in repo/logs/output) — it means the leaked-credential risk stays open a while longer. **Must be re-flagged before R5 go-live** (production env vars, DNS cutover, and real Resend sending all depend on rotated credentials — Spec §2.4, §10). The agent confirms-not-performs this step (Spec §5.0); until the operator asserts rotation done, R0–R4 proceed on file-DB/mocked-email environments where the distinction is moot (Spec §4.5).
- **0.2 (branch protection on `main`) is unavailable on this GitHub plan** — no paid tier / protection feature on this account. Substitute control: PR-only discipline (CLAUDE.md rule 9 — one milestone branch at a time, Conventional Commits, every commit passes `npm run check`, PRs only, never commit to `main`) plus operator review of every PR before merge. This is a process control, not a technical guardrail, so it depends on the operator and agent actually observing it every time — no CI-enforced blocker exists if it's skipped.
  Why: operator ruling, 2026-07-04 session (both raised as blocking-looking Phase 0 items that are intentionally not blocking). Record and continue; do not stall R0 on either.

**014 · 2026-07-04 · R0 step 2 (reconcile with origin) surfaced further next.js CVEs beyond the merged fix; bumped past what the merge alone provided.** `origin/main`'s merge set `next` to `15.3.6` (the CVE-2025-66478 fix), but `npm install` immediately flagged many further disclosed advisories against that version, several HIGH severity (DoS via Server Components, SSRF via WebSocket upgrades, middleware/proxy bypass). Operator chose (asked live, not a smallest-reasonable-choice default): bump to `next@15.5.20` (npm's non-major fix target) now, in R0, rather than defer. `npm audit fix` (non-breaking) also cleared a transitive `ws` DoS/memory-disclosure issue. Verified: `npx tsc --noEmit` clean, `npm run build` green (552 pages, test-runner-confirmed) after the bump.
Left untouched, deliberately out of scope for this step: `next-auth` (Iron rule 7 — stays exactly pinned, not part of this bump); `drizzle-orm`/`drizzle-kit` (moderate/high advisories remain, but bumping the ORM touches the live production DB layer — Iron Invariant 1 — and deserves its own decision, not one bundled into a Next.js reconciliation); one residual moderate `next`/`postcss` advisory has no non-major fix path available in the 15.x line and was left as-is.

**015 · 2026-07-04 · R0 tooling setup: first-ever lint pass found pre-existing violations in the stranded collection code; fixed the trivial ones, deferred the architectural one.** ESLint (`eslint-config-next` flat config) had never run on this repo. First pass found 1 error (`next-env.d.ts` triple-slash reference — a Next-generated file, added to eslint ignores) and 11 warnings. Fixed directly (zero-risk, no behavior change): unused `getAllPaintings` import, unused `mediums`/`activeMuralId` destructured props (interfaces/callers untouched — kept for documented future use), a misplaced `eslint-disable-next-line` comment in `MuralMap.tsx` that was 6 lines above the line it was meant to suppress, and an anonymous default export in `postcss.config.mjs`. Deferred: `@next/next/no-img-element` fired 3 times in the collection feature (`ArtworkCard.tsx`, `page.tsx`, painting detail page) — this is exactly R2's job (Spec §7: `next/image` + `artUrl()` + Blob), and no `artUrl()` helper exists yet. **Correction (spec-auditor caught this):** a repo-wide rule disable is a Spec §13.2 escalation trigger ("disable a lint rule repo-wide"), not an ordinary judgment call — the first version of this entry turned the rule off in `eslint.config.mjs` without operator sign-off, which was wrong. Replaced with a per-line `eslint-disable-next-line @next/next/no-img-element` at each of the 3 sites, each with a comment pointing to R2 — this defers the same work without disabling the rule for the rest of the codebase, so it no longer trips the escalation trigger at all. Also reverted Prettier's mechanical reformatting of `CLAUDE.md` and `OPERATOR-GUIDE.md` — those two root docs are outside the agent's maintenance mandate (only `PROGRESS.md`/`DECISIONS.md`/`README.md` are). Added both to `.prettierignore` (beyond Spec §5.1.3's named `docs/`/`drizzle/`/`public/`) so future `format`/`format:check` runs (including CI) can't touch them again — relying on the agent simply not re-running the command against them wasn't durable.

**016 · 2026-07-04 · R0 coverage gate scoped to exactly `trail-service.ts` + `art-service.ts`, not the whole `src/lib/**` glob.** Spec §4.2 states the general target as "80% lines/functions on `src/lib/**` and `src/app/api/**`" but also says R0 specifically sets the gate "no lower than 80 for the trail+art services," and §5.1.4's R0 test list names exactly `trail-service`, `art-service`, and redemption-code format — nothing about `trail-emails.ts`, `mural-data.ts`, or the API routes. Running coverage with the full `src/lib/**` glob included pulled in two untested files that don't belong in R0's scope: `trail-emails.ts` (0% — R1 rewrites it per §6.1, testing pre-rewrite code is throwaway) and `mural-data.ts` (0% — a static placeholder array pending real R4 content, not "logic that can be wrong" in the sense §4.2 cares about). `vitest.config.ts`'s `coverage.include` is `['src/lib/trail-service.ts', 'src/lib/art-service.ts']` for R0. Each of the deferred files joins the gate once it has milestone-appropriate tests: `trail-emails.ts` in R1, `mural-data.ts` whenever it's worth a sanity test (real content lands R4), API routes in R1 alongside their integration-test layer (§4.4).

**017 · 2026-07-04 · Local dev machine's Node upgraded 20.17.0 → 20.19.0 (operator-approved, live).** Setting up the vitest scaffold (task 3) surfaced a hard failure: `npx vitest run` couldn't even load `vitest.config.ts` — `ERR_REQUIRE_ESM` on `std-env`'s ESM build, because Node's `require(esm)` support wasn't stabilized-by-default until 20.19 (this machine had 20.17.0 and 20.11.1 via nvm4w, both too old; the `eslint-visitor-keys@5.0.1` `EBADENGINE` warning from the tooling install earlier this session was the same root cause showing up early). This isn't a code defect — current vitest 4.x / ESLint 9.x's own dependency chain now assumes a newer Node 20 patch than Spec §2.1's "Node 20 LTS (≥20)" literally requires. Asked the operator live (this changes the default Node version machine-wide via nvm4w, affecting any other project using the default, so it wasn't a call to make silently): operator chose to install and switch now (`nvm install 20.19.0 && nvm use 20.19.0`) rather than work around it per-session with `NODE_OPTIONS=--experimental-require-module`. Verified after the switch: `npx vitest run --coverage`, `npx tsc --noEmit`, and `npx eslint . --max-warnings 0` all pass cleanly with no flags. CI is unaffected either way — `actions/setup-node@v4` with `node-version: 20` pulls a current 20.x patch, already well past 20.19.

**018 · 2026-07-04 · `.github/workflows/ci.yml` excluded from Prettier.** Spec §4.3 mandates the CI workflow's content "verbatim," including double-quoted YAML strings and a multi-space-aligned inline comment — but Prettier (singleQuote: true, per DECISIONS 015's `.prettierrc`) rewrites both, which made `npm run check`'s `format:check` step fail against the file exactly as specified. The file-content instruction (§4.3, names an exact file with exact text) is more specific than the general "run Prettier repo-wide" rule (§5.1.3), per the Spec's own conflict rule ("more specific instruction wins" — §0.1). Added `.github/workflows/ci.yml` to `.prettierignore` rather than reformat the spec's verbatim YAML or leave the R0 gate red — same pattern as the `CLAUDE.md`/`OPERATOR-GUIDE.md`/`scripts/art-data.json` exclusions (DECISIONS 015).

**019 · 2026-07-04 · CI caught a real build bug: eager `Resend` client construction crashed `next build` with no `RESEND_API_KEY`.** First actual CI run on PR #3 failed at "Collecting page data" — `src/lib/trail-emails.ts` built a `new Resend(process.env.RESEND_API_KEY)` client at module scope, which throws immediately if the key is undefined. CI intentionally has no `RESEND_API_KEY` (Invariant 4), and `next build` imports every route module (including the checkin route, which imports `trail-emails.ts`) during page-data collection — so the crash was guaranteed, just never observed locally because `.env.local` has a key set. This is a genuine code defect (not a test-scope call): §5.2's R0 gate explicitly requires "CI green on the PR," so it had to be fixed in R0 despite `trail-emails.ts` otherwise being out of R0's test/coverage scope (DECISIONS 016, R1 rewrites it). Fix: lazy client construction (`getResendClient()`, built on first actual send call, not at import time) — no behavior change, just makes the module import side-effect-free. Added one regression test (`tests/lib/trail-emails.test.ts`) asserting the module imports cleanly with `RESEND_API_KEY` unset; verified the fix directly under CI's exact conditions (temporarily renamed `.env.local` out of the way, ran `db:seed-ci && build` with only CI's env vars set, confirmed pass, restored `.env.local` immediately after).

**020 · 2026-07-04 · R0 baseline-migration "mark as applied" command produced for the operator (not run against production — Iron Invariant 1).** OPERATOR-GUIDE §R0 promised "the agent will give you a one-time command"; this closes that loose end before R1's own production migration. Derived the exact `hash`/`created_at` values `drizzle-orm/libsql/migrator`'s `migrate()` would write by running it once against a throwaway scratch file DB (never touched `drizzle/`, `schema.ts`, or production) and reading back `__drizzle_migrations`. The command (below) creates that same tracking table on production and inserts the identical row, so R1's real `drizzle-kit migrate` sees the baseline as already applied and only runs its own new migration. Given to the operator to run themselves, after their own backup, per OPERATOR-GUIDE §R1 step 1 — never run by the agent.

```sql
CREATE TABLE IF NOT EXISTS "__drizzle_migrations" (
	id SERIAL PRIMARY KEY,
	hash text NOT NULL,
	created_at numeric
);

INSERT INTO "__drizzle_migrations" ("hash", "created_at")
VALUES ('ab3fac3df65bc18183f2da9f7f494de6660ca9e1623e487f9f1b6a0072af02c7', 1783181505344);
```

**021 · 2026-07-04 · R1 step 0 (carry-forward `style:` reformat commit) is a no-op — the reformat was already merged in R0, so it is skipped.** The approved R1 plan and PROGRESS.md's "Post-R0 note" both assumed the repo-wide Prettier reformat (65 files, new `.prettierrc`) was sitting **unstaged** on `main`, to be committed as a standalone `style:` commit at the start of `r1-trail`. Reality on inspection: R0 was merged into `main` (PRs #3 and #4), and that merge already captured the reformat — `git show HEAD:.prettierrc` is the new ruleset (`singleQuote`, `printWidth: 100`, `trailingComma: all`) and `npx prettier --check .` reports "All matched files use Prettier code style!" on a fresh `r1-trail`. The 64 non-`PROGRESS.md` files that show as `M` in `git status` have **empty content diffs** — they differ only by CRLF/LF under `core.autocrlf=true` (the "LF will be replaced by CRLF" warnings), not by Prettier content. The one real uncommitted change is `PROGRESS.md`'s own "Post-R0 note", which is now stale (it describes the reformat as uncommitted); it will be superseded when PROGRESS.md is rewritten at R1 close. Net: no `style:` commit is created (committing it would be an empty/no-op or pure line-ending churn), R1 logic begins directly on a format-clean, gate-clean baseline.

**022 · 2026-07-04 · R1 mural-content honesty mechanism (extends DECISIONS 007).** Implements §4.4's suppression of fabricated mural content by making the DATA honest rather than adding a suppression flag or new field. Concretely, in `src/lib/mural-data.ts`: (1) each mural's `name` now holds its **real, verified location/business name** (the prefix of the already-geocoded `address`, e.g. "Lighthouse Cafe") — so every existing `mural.name` render (trail check-in cards, map popups, `/murals` + `/murals/trail` cards) shows a true label with **zero call-site changes**; (2) the fabricated `description` and `year` **values are removed** (the fields are already optional and every render already guards them with `&&`), so they simply stop rendering. This satisfies Iron Invariant 3 (no fiction presented as fact — anywhere public, per the operator's scope ruling this session) AND Architecture §7.3's "un-suppression by data presence, not code change": R4's intake fills `description`/`year` → they reappear via the existing guards, and overwrites `name` with the real artwork title → a pure data overwrite. Gallery emails already identify each stop by its verified `address` string (which leads with the location name), so no "Mural #0" or invented title can leak. Rejected the plan's literal "add a `locationName` field + make `name` optional + `name ?? locationName` fallbacks" — it's more code and more edited call-sites for the same visible outcome; Spec §0.2 prefers the boring, minimal change. The mural coordinates/addresses/radii (real since March 2026) are untouched. Operator note carried: more murals will be added later — all range checks derive from `MURAL_LOCATIONS.length`, never a hardcoded 14 (`trail-service`, checkin route).

**023 · 2026-07-04 · R1 coverage gate grown to the trail routes + `trail-emails.ts` (successor to DECISIONS 016).** R0 scoped `vitest.config.ts`'s `coverage.include` to just `trail-service.ts` + `art-service.ts` because the API routes and `trail-emails.ts` were about to be rewritten (testing throwaway code is waste). R1 rewrites them and lands their integration/unit tests, so they now join the enforced gate: `include` = `trail-service.ts`, `trail-emails.ts`, `art-service.ts`, `src/app/api/trail/checkin/route.ts`, `src/app/api/trail/status/route.ts`. Measured this session (thresholds `lines: 80` / `functions: 80`, run exits 0): checkin route 85.71/100, status route 80.00/100, trail-service 89.83/90, trail-emails 82.85/100, art-service 96/100. Thresholds unchanged and never lowered (Spec §4.2 / §13.2). Still deferred until milestone-tested: `mural-data.ts` (static data — has an honesty guard test now but no numeric gate until real R4 content) and the collection API routes (R3's verify-then-fix + Playwright layer, §4.4).

**024 · 2026-07-04 · R2 thumbnails render via `next/image` `fill`, not intrinsic `width`/`height`.** Architecture §6 says every artwork rendering goes through `next/image` "with real `width`/`height` (the DB has `width_px`/`height_px`)". Applied literally to the painting-detail hero (`painting/[slug]/page.tsx`), which renders at the image's natural aspect ratio — real `widthPx`/`heightPx` passed directly. The three thumbnail sites (`ArtworkCard.tsx`, home `page.tsx` and `collection/page.tsx` category cards) are different: they crop into a **fixed-size container** (`aspectRatio: 3/4` or fixed `height`) via `object-fit: cover`, and the category-card query (`getCategoryCards` in `art-service.ts`) only ever selects `thumbPath` — no width/height at all, so passing "real" dimensions there isn't possible without a second query just to get them. Used `fill` + `sizes` instead — the standard idiom for "crop into a container of unknown/mismatched aspect ratio" and the only option that doesn't require fabricating numbers. Rejected: adding widthPx/heightPx to `getCategoryCards()` just to satisfy the letter of the rule — extra query cost for a card that's always cropped anyway, no visible benefit. Net effect matches the Architecture's actual intent (§6's real point is "no guessed dimensions," which both approaches satisfy) — recorded because it's a judgment call on ambiguous wording, not because it's a deviation.

**025 · 2026-07-04 · R2 Blob `remotePatterns` hostname wildcarded.** `next.config.ts` images.remotePatterns uses `**.public.blob.vercel-storage.com` rather than a specific store subdomain, because the real store isn't provisioned until the operator's first `sync-art-blob.ts --apply` run (needs the Blob token, which the agent never holds). Once the store exists, the wildcard already matches it — no follow-up config change needed after the real upload.

**026 · 2026-07-04 · R2 image-budget e2e test seeds its own `file:./e2e-test.db`, isolated from `.env.local`.** Found `.env.local`'s `TURSO_DATABASE_URL` currently points at production Turso, not `file:./dev.db` as Spec §2.1 describes as the default dev mode (flagged to the operator; out of scope to fix here). `playwright.config.ts`'s `webServer.env` sets `TURSO_DATABASE_URL`/`TURSO_AUTH_TOKEN` explicitly rather than inheriting the shell/`.env.local` environment — Next.js never overwrites an already-set process env var from a `.env` file, so this override is guaranteed to win regardless of `.env.local`'s contents (Iron Invariant 1). The webServer command reuses the existing `npm run db:seed-ci` script (same 20-painting fixture CI already uses) rather than writing new seed logic, since fixture `web_image_path`/`thumb_path` values were verified to match real files under the local `public/art/` — giving the budget test honest byte counts without touching production data.

**027 · 2026-07-04 · R2 real Blob upload: local/CLI access needs a static token or explicit "Development" OIDC trust, and a fix commit missed the R2 merge window.** Two things worth recording for R4 (which will re-sync images) and future sessions: (1) Vercel's default Blob-store-to-project connection is OIDC-only, scoped to whichever environments you pick at connection time — the operator's store was initially Production/Preview only, so `sync-art-blob.ts --apply` run locally failed with `BlobOidcEnvironmentNotAllowedError` (local runs present as "development"). Fixed by the operator via the store's "Update Project Connection" dialog: checked "Development" and also "Add a read-write token env var to this connection" (a static `BLOB_READ_WRITE_TOKEN` — the more portable fix, since it doesn't depend on per-environment OIDC trust at all). (2) `scripts/sync-art-blob.ts` originally didn't load `.env.local` (unlike its sibling `migrate-art-data.ts`) and its `--apply` guard only recognized `BLOB_READ_WRITE_TOKEN`, not the OIDC pair (`VERCEL_OIDC_TOKEN` + `BLOB_STORE_ID`) — fixed same-session (commit `74bd44e`), but that commit was pushed to `origin/r2-images` _after_ the operator had already clicked merge on PR #6 (merge `5085a22` at 16:28:48; fix commit at 16:49:38) — a timing crossover, not a process violation, but it meant the fix never made it into `main` via PR #6 itself and needed its own follow-up (branch `chore/r2-followup`). Real upload succeeded regardless because the working tree used to run it already had the fix locally. Real upload result: 1056/1056 files, 0 errors, ~202MB, verified serving correctly on a redeployed Vercel preview before the operator merged.

---

**028 · 2026-07-04 · R3 kickoff: fixed `.env.local`'s `TURSO_DATABASE_URL` (flagged but left open by DECISIONS 026) + documented the `db:seed-ci`/build env-var split.** Two related environment fixes before starting R3 task work: (1) `.env.local` had two duplicate `TURSO_DATABASE_URL` lines, both pointing at production Turso — deduped to one line, set to `file:./dev.db` per Spec §2.1's stated default (production creds kept commented-out immediately below for the rare R4/R5 task that needs them). (2) Discovered `scripts/seed-ci.ts` does **not** load `.env.local` (`tsx` has no built-in dotenv loading, unlike `next build`/`next dev`/`next start`) — it falls back to its own hardcoded `file:./ci.db` default regardless of `.env.local`'s contents. So the Spec's own gate shorthand (`npm run db:seed-ci; npm run build`) only produces a working, seeded build locally when `TURSO_DATABASE_URL=file:./ci.db` is also exported in the shell for that invocation — otherwise `build`/`start` read `.env.local`'s `dev.db` (empty, unseeded) while `db:seed-ci` populated a completely different file. Verified the fix by running `TURSO_DATABASE_URL=file:./ci.db npm run db:seed-ci && npm run build` end to end — succeeds against the seeded fixture, matches CI's own explicit job-level env var (Spec §4.3). Every R3 task's manual build/start verification step must export this same override; recorded here once rather than repeating it in every task.

**029 · 2026-07-05 · R3 task 3: `CATEGORY_TAG_MAP.florals` fused tag string split, verified rather than assumed.** `src/lib/constants.ts`'s `florals` entry (line 148) held `'LillyOther plants'` — two tag names fused together by a prior extraction bug, named explicitly in Architecture §5.2.3 along with its expected fix. Per this task's operator-assisted verification step, the operator ran `SELECT name FROM tags WHERE name LIKE '%lil%' OR name LIKE '%plant%' COLLATE NOCASE;` against production Turso (read-only; the agent never touched production) and confirmed exactly two rows: `Lily` and `Other plants` — matching Architecture §5.2.3's stated expectation exactly, not the fused-string-in-production scenario. Fixed `CATEGORY_TAG_MAP.florals.tags` to list `'Lily'` and `'Other plants'` as two separate entries. Added `tests/lib/constants.test.ts` as a regression guard: (1) an explicit assertion that `florals.tags` no longer contains `'LillyOther plants'` and does contain both split names, and (2) a map-integrity test that seeds a file-DB `tags` table with a frozen, independently-written list of every currently-verified tag name (not derived from `CATEGORY_TAG_MAP` itself, to avoid a circular check) and asserts every tag `CATEGORY_TAG_MAP` references exists in it — so a future edit that reintroduces a fused string, typo, or otherwise-unverified tag name fails loudly instead of silently emptying a category. TDD-verified: test failed for the right reason (fused string not in the known-good list) before the fix, passed (2/2) after.

_(Build sessions append from 021 onward.)_
